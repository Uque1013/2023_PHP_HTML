<<css 형식
selector {속성:값; 속성:값;...}
1. 태그
2. class(.)
	1) 생성: .클래스이름{.....}
	2) 사용: class=클래스이름
3. Id(#)

반응형웹(lesponsne web)
- 디바이스에 맞춰서 만든 웹, 모든 기기에 맞춰서 웹을 만듦
- ininial-scale=1.0 : 웹을 디바이스 크기에 맞춰서 지원함(0.5면 디바이스 크기에 절반만 맞춰서 보여줌)
- 이 문서에 대한 정보를 나타내는 코드(mata)